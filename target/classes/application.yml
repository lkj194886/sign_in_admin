server:
  port: 8090
spring:
  datasource:
    name: sign_in_db
    url: jdbc:mysql://47.114.159.156:3306/sign_in_db?serverTimezone=CTT&useUnicode=true&characterEncoding=utf-8&useSSL=false&allowMultiQueries=true
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver
    hikari:
      minimum-idle: 5
      idle-timeout: 180000
      maximum-pool-size: 10
      auto-commit: true
      pool-name: sign_in_db
      max-lifetime: 1800000
      connection-timeout: 30000
      connection-init-sql: SELECT 1
  redis:
    database: 0
    host: localhost
    port: 6379
    password:
    timeout: 30
    jedis:
      pool:
        max-active: 200
        max-idle: 10
        min-idle: 10
        max-wait: 10000
mybatis:
  mapper-locations: classpath*:/mapper/*.xml
#分页配置
pagehelper:
  helper-dialect: mysql
  reasonable: false
  support-methods-arguments: true
  params: count=countSql
file:
  path: file/**    #文件访问路径
  address:  D://wallpaper/  #文件保存路径


#redis